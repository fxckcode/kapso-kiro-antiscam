{
  "name": "antiscambot-backend",
  "version": "0.1.0",
  "private": true,
  "description": "AntiScamBot backend - Kapso/WhatsApp integration, SQS ingest and processor",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@strands-agents/sdk": "1.11.1",
    "@aws-sdk/client-dynamodb": "^3.658.0",
    "@aws-sdk/client-kms": "^3.658.0",
    "@aws-sdk/client-secrets-manager": "^3.658.0",
    "@aws-sdk/client-sqs": "^3.658.0",
    "@aws-sdk/lib-dynamodb": "^3.658.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.0"
  }
}
